extends  ../layout
block content
    .container
        .row
          form.form-group(method="post",action="/admin/category")           
            .form-group
                label.colsm-3.col-form-label(for="inputCategory") 电影分类
                .colsm-10
                  input#inputCategory.form-control(type="text",name="category[name]",value=category.name)         
            .form-group
                .col-sm-offset-2.col-sm-10
                  button.btn.btn-default(type="submit") 录入